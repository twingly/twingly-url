source 'https://rubygems.org/'

gemspec

# because we can't do "require: false" in the .gemspec file
group :test, :development do
  gem "simplecov", require: false
  gem "coveralls", require: false
end
